<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.BoardDAO">

	<select id="selectList" resultType="BoardVO">
		SELECT b.*, v.board_idx
		FROM board b
		LEFT JOIN view v ON b.board_idx = v.board_idx
		WHERE b.board_sort = '공지사항';
	</select>
	
	<select id="selectOne" parameterType="int" resultType="BoardVO">
		SELECT * FROM board WHERE board_idx=#{ board_idx }
	</select>
	
		

</mapper>